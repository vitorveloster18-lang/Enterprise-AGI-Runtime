# Limites de tempo por task (o regulamento permite definir; schema desconhecido).
# VERIFICAR contra HARNESS_README.md — se o formato estiver errado, remover o arquivo.
per_task_timeout_seconds: 1800
max_patch_rounds: 3
